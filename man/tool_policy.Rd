% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_policy.R
\name{tool_policy}
\alias{tool_policy}
\title{Define authorization and limits for model tools}
\usage{
tool_policy(
  allowed_tools = character(),
  schemas = list(),
  authorize = NULL,
  validators = list(),
  side_effect_tools = character(),
  max_calls = Inf,
  max_side_effects = 0L,
  spend_limits = numeric(),
  spend_argument = "amount",
  show_stats = FALSE
)
}
\arguments{
\item{allowed_tools}{Explicit tool allowlist.}

\item{schemas}{Named list of compact JSON-Schema-like lists or validator
functions. Supported schema fields are \code{required}, \code{properties}, and
\code{additionalProperties}; property fields include \code{type}, \code{enum}, \code{pattern},
\code{minimum}, and \code{maximum}.}

\item{authorize}{Optional function receiving \verb{(subject, tool_name, arguments)}.}

\item{validators}{Named list of additional per-tool validator functions.}

\item{side_effect_tools}{Tools counted against \code{max_side_effects}.}

\item{max_calls}{Maximum tool requests in one guarded chat.}

\item{max_side_effects}{Maximum side-effecting requests in one guarded chat.}

\item{spend_limits}{Named numeric vector of maximum spend per tool.}

\item{spend_argument}{Name of the numeric argument carrying spend.}

\item{show_stats}{Show construction time and available usage metrics.}
}
\value{
A \code{shieldr_tool_policy} object.
}
\description{
Tool policy combines a default-deny allowlist with per-tool argument schemas,
subject authorization, custom validators, and call, side-effect, and spend
limits. Custom validators receive \verb{(arguments, subject, tool_name)} and must
return \code{TRUE}, \code{FALSE}, a message, or \code{list(valid, message)}.
}
\examples{
tools <- tool_policy(
  allowed_tools = "search_docs",
  schemas = list(search_docs = list(
    required = "query",
    properties = list(query = list(type = "string")),
    additionalProperties = FALSE
  )),
  max_calls = 3
)
}
