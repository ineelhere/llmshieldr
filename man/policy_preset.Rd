% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policy.R
\name{policy_preset}
\alias{policy_preset}
\title{Load a policy preset}
\usage{
policy_preset(name, overrides = list())
}
\arguments{
\item{name}{One of \code{"enterprise_default"}, \code{"pharma_gxp"},
\code{"finance_strict"}, \code{"education_safe"}, \code{"open_research"},
\code{"comprehensive"}, \code{"custom"}, or \code{"baseline"}. \code{"baseline"} is a
backward-compatible alias for \code{"enterprise_default"}.}

\item{overrides}{Optional list with \code{rules}, \code{thresholds}, \code{rate_guard}, or
\code{trusted_sources} entries.}
}
\value{
A \code{shieldr_policy}.
}
\description{
Creates a hard-coded policy preset covering common industry profiles and
OWASP LLM Top 10 categories.
}
\details{
Presets are assembled from the built-in rule helpers in \code{R/rules.R}. They
use OWASP GenAI / LLM Top 10 categories as the organizing taxonomy, then add
common operational controls such as PII patterns, secret patterns,
system-prompt extraction checks, excessive-agency language, domain-specific
claims, and optional rate guards.

Preset source model:
\itemize{
\item \code{enterprise_default}: broad production baseline for injection, PII,
secrets, system prompt extraction, and agency language.
\item \code{pharma_gxp}: \code{enterprise_default} plus clinical identifiers, diagnosis
and treatment language, unsafe code checks, and stricter thresholds.
\item \code{finance_strict}: \code{enterprise_default} plus account numbers, investment
advice language, autonomous trading language, and a cost/token rate guard.
\item \code{education_safe}: \code{enterprise_default} plus minor-related PII and
academic-integrity bypass language.
\item \code{open_research}: a smaller open-workflow profile focused on injection and
secrets, with higher thresholds.
\item \code{comprehensive}: a maximum-coverage profile combining the enterprise,
pharma, finance, education, code-safety, and rate-guard controls.
\item \code{custom}: no rules, default thresholds.
\item \code{baseline}: backward-compatible alias for \code{enterprise_default}.
}

The presets are starting points. They are transparent, testable, and meant
to be extended with \code{\link[=add_rule]{add_rule()}} for application-specific requirements.
}
\examples{
policy_preset("enterprise_default")
policy_preset("open_research", overrides = list(thresholds = list(redact_at = 0.7)))
}
