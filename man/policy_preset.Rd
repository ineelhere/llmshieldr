% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policies.R
\name{policy_preset}
\alias{policy_preset}
\title{Load a Policy Preset}
\usage{
policy_preset(name)
}
\arguments{
\item{name}{Character. One of:
\itemize{
\item \code{"pharma_gxp"} — Pharmaceutical GxP-compliant preset. Strictest
thresholds. Includes PHI/CDISC-specific rules, efficacy claim checks,
and diagnosis blocking.
\item \code{"enterprise_default"} — General enterprise preset. Covers secrets,
common PII, and injection rules with standard thresholds.
\item \code{"finance_guard"} — Financial services preset. Adds financial advice
detection and credit card patterns.
\item \code{"legal_guard"} — Legal industry preset. Adds legal opinion
detection rules.
}}
}
\value{
A named list with elements:
\describe{
\item{\code{name}}{Character. The policy name.}
\item{\code{rules}}{A list of rule objects applicable to this policy.}
\item{\code{thresholds}}{A named list with \code{block}, \code{redact}, and \code{warn}
score thresholds.}
}
}
\description{
Returns a named policy configuration containing the rules and thresholds
appropriate for a specific regulatory or industry context.
}
\examples{
# Load the pharma GxP policy
policy <- policy_preset("pharma_gxp")
policy$name
length(policy$rules)
policy$thresholds

# Load the enterprise default policy
policy <- policy_preset("enterprise_default")
length(policy$rules)

# Use a policy with scan_prompt
report <- scan_prompt(
  "Patient USUBJID-042 enrolled at site.",
  policy = policy_preset("pharma_gxp")
)
report$action

}
\seealso{
\code{\link[=scan_prompt]{scan_prompt()}}, \code{\link[=secure_chat]{secure_chat()}}, \code{\link[=decide_action]{decide_action()}}
}
