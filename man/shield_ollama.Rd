% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ollama.R
\name{shield_ollama}
\alias{shield_ollama}
\title{Guard a Local Ollama Chat}
\usage{
shield_ollama(
  prompt,
  policy = policy_preset("enterprise_default"),
  context = NULL,
  action = c("auto", "redact", "warn", "block"),
  checks = c("rules", "llm", "both"),
  model = "gemma3:4b",
  review_model = model
)
}
\arguments{
\item{prompt}{A single character string containing the user prompt.}

\item{policy}{A policy list from \code{\link[=policy_preset]{policy_preset()}}. Defaults to
\code{enterprise_default} for a practical general-purpose setup.}

\item{context}{Optional character vector or data frame of context
documents to append after scanning.}

\item{action}{Character. Override the scanner's recommended action. One of
\code{"auto"}, \code{"redact"}, \code{"warn"}, or \code{"block"}.}

\item{checks}{Character. One of \code{"rules"}, \code{"llm"}, or \code{"both"}.}

\item{model}{Character. Ollama model for the user-facing assistant.}

\item{review_model}{Character. Ollama model for the reviewer. Defaults to
\code{model}.}
}
\value{
A \code{secure_result} S3 object.
}
\description{
Creates local Ollama chat clients through \code{ellmer::chat_ollama()} and runs
\code{\link[=secure_chat]{secure_chat()}} with a private reviewer workflow that stays on your machine.
}
\details{
This is the easiest way to use \code{llmshieldr} with Ollama and \verb{gemma3:4b}
without manually creating separate assistant and reviewer objects.
}
\examples{
\dontrun{
library(llmshieldr)

# In a shell first: ollama pull gemma3:4b
result <- shield_ollama(
  prompt = "Explain this dplyr error and suggest a fix.",
  policy = policy_preset("enterprise_default"),
  checks = "both",
  model = "gemma3:4b"
)

result$output
result$risk_summary
}

}
\seealso{
\code{\link[=secure_chat]{secure_chat()}}, \code{\link[=scan_prompt]{scan_prompt()}}, \code{\link[=llm_review]{llm_review()}}
}
