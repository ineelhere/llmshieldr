% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{list_rules}
\alias{list_rules}
\title{List Active Rules}
\usage{
list_rules(custom_only = FALSE)
}
\arguments{
\item{custom_only}{Logical. If \code{TRUE}, returns only rules added via
\code{\link[=add_rule]{add_rule()}}. Default is \code{FALSE} (returns all active rules).}
}
\value{
A list of rule objects.
}
\description{
Returns the full active rule set or only custom (user-added) rules.
}
\examples{
# List all active rules
rules <- list_rules()
length(rules)

# List rule IDs
vapply(list_rules(), `[[`, character(1), "id")

# List only custom rules (empty if none added)
list_rules(custom_only = TRUE)

}
\seealso{
\code{\link[=add_rule]{add_rule()}}, \code{\link[=remove_rule]{remove_rule()}}, \code{\link[=policy_preset]{policy_preset()}}
}
