% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_policy.R
\name{guard_tool}
\alias{guard_tool}
\title{Dispatch one tool through guardrails}
\usage{
guard_tool(
  tool_name,
  arguments = list(),
  dispatcher,
  tool_policy,
  subject = NULL,
  policy = "enterprise_default",
  reviewer = NULL,
  checks = "rules",
  redaction = NULL,
  scanners = scanner_options(),
  show_tokens = FALSE,
  show_stats = FALSE
)
}
\arguments{
\item{tool_name}{Tool name.}

\item{arguments}{Named argument list.}

\item{dispatcher}{Function accepting \verb{(tool_name, arguments)}, or a named
list of tool functions called with \code{do.call()}.}

\item{tool_policy}{Policy from \code{\link[=tool_policy]{tool_policy()}}.}

\item{subject}{Optional authorization context passed through unchanged.}

\item{policy}{A \code{shieldr_policy} or built-in policy name.}

\item{reviewer}{Optional reviewer function or object with \verb{$chat()}.}

\item{checks}{One of \code{"rules"}, \code{"nlp"}, \code{"llm"}, or \code{"both"}.}

\item{redaction}{Optional redaction strategy from \code{\link[=redaction_strategy]{redaction_strategy()}}.}

\item{scanners}{Optional scanner configuration from \code{\link[=scanner_options]{scanner_options()}}.}

\item{show_tokens}{Whether to attach token counts when \code{ellmer} is available.}

\item{show_stats}{Show execution statistics as messages.}
}
\value{
A list with \code{action}, \code{value}, \code{call_report}, and \code{output_report}.
}
\description{
Scans arguments, executes only an allowed request, then scans the tool result
before returning it. This is the provider-neutral dispatcher for chat SDKs
that do not expose request/result hooks.
}
\examples{
dispatcher <- list(search = function(query) paste("result", query))
guard_tool(
  "search", list(query = "public"), dispatcher,
  tool_policy(allowed_tools = "search")
)
}
