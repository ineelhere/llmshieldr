% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streaming.R
\name{stream_guard}
\alias{stream_guard}
\title{Create a stateful safe-release stream guard}
\usage{
stream_guard(
  emit,
  cancel = NULL,
  policy = "enterprise_default",
  reviewer = NULL,
  checks = "rules",
  overlap = 200L,
  redaction = NULL,
  scanners = scanner_options(),
  show_tokens = FALSE,
  show_stats = FALSE
)
}
\arguments{
\item{emit}{Function receiving the final cleaned text after an allow/redact
decision.}

\item{cancel}{Optional function called when the guard blocks or is cancelled.}

\item{policy}{A \code{shieldr_policy} or built-in policy name.}

\item{reviewer}{Optional reviewer function or object with \verb{$chat()}.}

\item{checks}{One of \code{"rules"}, \code{"nlp"}, \code{"llm"}, or \code{"both"}.}

\item{overlap}{Number of trailing characters from prior output to include
when scanning the next chunk.}

\item{redaction}{Optional redaction strategy from \code{\link[=redaction_strategy]{redaction_strategy()}}.}

\item{scanners}{Optional scanner configuration from \code{\link[=scanner_options]{scanner_options()}}.}

\item{show_tokens}{Whether to attach token counts when \code{ellmer} is available.}

\item{show_stats}{Show construction time and available usage metrics.}
}
\value{
A \code{shieldr_stream_guard} environment with \verb{$push(chunk)}, \verb{$finish()},
\verb{$cancel()}, and \verb{$status()} methods. Each method accepts \code{show_stats}.
}
\description{
Buffers provider chunks and releases text only after \code{\link[=scan_stream]{scan_stream()}} has made
a final decision over the complete response. This conservative contract
prevents split payloads from reaching the consumer and makes cancellation
explicit. It trades token-by-token display for a release guarantee; callers
must send raw provider chunks only to \verb{$push()}.
}
\examples{
released <- character()
guard <- stream_guard(function(text) released <<- c(released, text))
guard$push("Contact ")
guard$push("a@example.com")
result <- guard$finish()
}
