% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shield_gemini.R
\name{shield_gemini}
\alias{shield_gemini}
\title{Guard a Gemini Developer API chat workflow (deprecated)}
\usage{
shield_gemini(
  prompt,
  policy = "enterprise_default",
  checks = "both",
  model = "gemini-2.5-flash",
  reviewer_model = "gemini-2.5-flash-lite",
  context = NULL,
  context_authorize = NULL,
  redaction = NULL,
  scanners = scanner_options(),
  show_tokens = FALSE,
  show_stats = FALSE,
  audit_content = c("metadata", "full")
)
}
\arguments{
\item{prompt}{User prompt.}

\item{policy}{A \code{shieldr_policy} or built-in policy name.}

\item{checks}{One of \code{"rules"}, \code{"nlp"}, \code{"llm"}, or \code{"both"}.}

\item{model}{Explicit Gemini assistant model name.}

\item{reviewer_model}{Explicit Gemini semantic reviewer model name.}

\item{context}{Optional data frame of retrieved context.}

\item{context_authorize}{Optional row authorization function.}

\item{redaction}{Optional redaction strategy.}

\item{scanners}{Optional scanner configuration.}

\item{show_tokens}{Whether to attach token counts.}

\item{show_stats}{Show execution statistics as messages.}

\item{audit_content}{\code{"metadata"} (default) or \code{"full"}.}
}
\value{
A \code{shieldr_result}.
}
\description{
This compatibility wrapper delegates to \code{\link[=secure_chat]{secure_chat()}} with
\code{provider = "gemini"}. New code should use that common provider path.
It requires the suggested \code{ellmer} package and \code{GEMINI_API_KEY} or
\code{GOOGLE_API_KEY} in the environment. No request is made until this function
is called.
}
\details{
The compatibility defaults retain the Gemini 2.5 model names used when this
wrapper was introduced. Google may limit those models for new projects; new
code should pass a current model explicitly to \code{\link[=secure_chat]{secure_chat()}}. Google
determines free-tier eligibility, quotas, model availability, and data-use
terms by account, project, and region. Check
\url{https://ai.google.dev/gemini-api/docs/pricing} and
\url{https://ai.google.dev/gemini-api/docs/rate-limits} for your project.
Gemini requests transmit the cleaned prompt and admitted context to Google.
Google's current pricing table marks free-tier content as eligible for
product improvement. Review current terms and organizational data rules
before sending private content.
}
\examples{
\dontrun{
# Set GEMINI_API_KEY in your environment before running.
result <- shield_gemini(
  "Summarize this public note.",
  model = "gemini-3.8-flash",
  reviewer_model = "gemini-3.5-flash-lite",
  show_stats = TRUE
)
result$output
}
}
