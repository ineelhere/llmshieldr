% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redact.R
\name{redact_text}
\alias{redact_text}
\title{Redact Risky Content from Text}
\usage{
redact_text(text, findings)
}
\arguments{
\item{text}{A single character string to sanitise.}

\item{findings}{A list of rule objects (as returned by detector functions).
Each rule must contain \code{pattern} and \code{mask} fields.}
}
\value{
A named list with two elements:
\describe{
\item{\code{text}}{The redacted text string.}
\item{\code{redaction_log}}{A list of lists, each containing \code{rule_id},
\code{original_match}, and \code{mask} for every replacement made.}
}
}
\description{
Replaces matched patterns in \code{text} with typed redaction masks (e.g.,
\verb{[REDACTED_API_KEY]}, \verb{[REDACTED_EMAIL]}). Returns both the cleaned text
and a redaction log for audit purposes.
}
\examples{
# Redact an email address
findings <- detect_pii_phi("Contact: jane.doe@pharma.com")
result <- redact_text("Contact: jane.doe@pharma.com", findings)
result$text
result$redaction_log

# Redact a subject identifier
findings <- detect_pii_phi("Patient USUBJID was enrolled.")
result <- redact_text("Patient USUBJID was enrolled.", findings)
result$text

# No findings — text unchanged
result <- redact_text("Safe prompt about SDTM.", list())
result$text

}
\seealso{
\code{\link[=scan_prompt]{scan_prompt()}}, \code{\link[=secure_chat]{secure_chat()}}
}
