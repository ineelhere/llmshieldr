% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/url_policy.R
\name{url_policy}
\alias{url_policy}
\title{Configure URL destination policy}
\usage{
url_policy(
  allowed_schemes = "https",
  allowed_hosts = NULL,
  blocked_hosts = c("localhost", "localhost.localdomain"),
  allow_userinfo = FALSE,
  allow_idn = FALSE,
  block_private = TRUE,
  max_redirects = 0L,
  show_stats = FALSE
)
}
\arguments{
\item{allowed_schemes}{Allowed schemes.}

\item{allowed_hosts}{Optional exact host allowlist.}

\item{blocked_hosts}{Exact blocked hosts. Subdomains are also blocked.}

\item{allow_userinfo}{Whether \code{user:password@host} authorities are allowed.}

\item{allow_idn}{Whether non-ASCII and punycode host names are allowed.}

\item{block_private}{Whether loopback, link-local, and private IP targets are
blocked, including executor-supplied DNS results.}

\item{max_redirects}{Maximum redirect targets accepted by \code{\link[=scan_url_target]{scan_url_target()}}.}

\item{show_stats}{Show construction time and available usage metrics.}
}
\value{
A \code{shieldr_url_policy} object.
}
\description{
Defines canonical URL checks for text scanners and for application code that
is about to make a network request. The policy can also validate resolved IP
addresses and redirect targets supplied by the network executor.
}
\examples{
policy <- url_policy(allowed_hosts = "api.example.com")
scan_url_target("https://api.example.com/v1", policy)
}
