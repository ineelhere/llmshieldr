% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{add_rule}
\alias{add_rule}
\title{Add a Custom Rule}
\usage{
add_rule(rule)
}
\arguments{
\item{rule}{A named list with required fields: \code{id}, \code{type}, \code{pattern},
\code{severity}, \code{action}, \code{mask}, \code{description}, \code{owasp}, \code{policy_tags}.}
}
\value{
Invisibly returns the updated list of custom rules.
}
\description{
Adds a user-defined rule to the active rule set. Custom rules persist
for the duration of the R session and are appended after the built-in
rule set.
}
\examples{
# Add a custom rule for internal project IDs
add_rule(list(
  id          = "internal_project_id",
  type        = "phi",
  pattern     = "PROJ-[A-Z]{3}-\\\\d{4}",
  severity    = 35,
  action      = "redact",
  mask        = "[REDACTED_PROJECT_ID]",
  description = "Internal project identifier detected",
  owasp       = "LLM02",
  policy_tags = c("enterprise_default")
))

# Verify it was added
custom <- list_rules(custom_only = TRUE)
length(custom)

# Clean up
remove_rule("internal_project_id")

}
\seealso{
\code{\link[=remove_rule]{remove_rule()}}, \code{\link[=list_rules]{list_rules()}}, \code{\link[=policy_preset]{policy_preset()}}
}
