% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rules.R
\docType{data}
\name{rule_bank}
\alias{rule_bank}
\title{Default Rule Bank}
\format{
A list of named lists, each representing one detection rule.
}
\usage{
rule_bank
}
\description{
The built-in rule bank containing all default detection rules used by
\code{llmshieldr}. Each rule is a named list with a standardised structure that
the detectors iterate over.
}
\details{
\subsection{Rule structure}{

Every rule is a plain list with the following fields:\tabular{lll}{
   Field \tab Type \tab Description \cr
   \code{id} \tab character \tab Unique rule identifier \cr
   \code{type} \tab character \tab Category: \code{"secret"}, \code{"phi"}, \code{"injection"}, or \code{"output"} \cr
   \code{pattern} \tab character \tab Regex pattern used for detection \cr
   \code{severity} \tab numeric \tab Weighted severity score (0–100) \cr
   \code{action} \tab character \tab Default action: \code{"block"}, \code{"redact"}, \code{"warn"}, or \code{"allow"} \cr
   \code{mask} \tab character \tab Replacement text for redaction \cr
   \code{description} \tab character \tab Human-readable finding description \cr
   \code{owasp} \tab character \tab OWASP Top 10 for LLMs risk tag \cr
   \code{policy_tags} \tab character vector \tab Policies this rule applies to \cr
}

}

\subsection{OWASP LLM Risk Categories}{
\itemize{
\item \strong{LLM01} — Prompt Injection
\item \strong{LLM02} — Sensitive Information Disclosure
\item \strong{LLM06} — Excessive Agency
\item \strong{LLM07} — System Prompt Leakage
\item \strong{LLM09} — Misinformation
}
}
}
\examples{
# Inspect the rule bank
length(rule_bank)

# View the first rule
str(rule_bank[[1]])

# List all rule IDs
vapply(rule_bank, `[[`, character(1), "id")

# Filter rules by type
secret_rules <- Filter(function(r) r$type == "secret", rule_bank)
length(secret_rules)

# Filter rules by OWASP tag
injection_rules <- Filter(function(r) r$owasp == "LLM01", rule_bank)
vapply(injection_rules, `[[`, character(1), "id")

}
\keyword{datasets}
