% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{shield_audit}
\alias{shield_audit}
\title{Create a Shield Audit Object}
\usage{
shield_audit(
  timestamp = Sys.time(),
  policy = "default",
  model = "unknown",
  provider = "unknown",
  input_report = NULL,
  output_report = NULL,
  final_action = "allow",
  redactions = list(),
  ...
)
}
\arguments{
\item{timestamp}{A \code{POSIXct} timestamp. Defaults to \code{\link[=Sys.time]{Sys.time()}}.}

\item{policy}{Character. The policy name that was applied.}

\item{model}{Character. The LLM model identifier.}

\item{provider}{Character. The provider class name.}

\item{input_report}{A \code{scan_report} from \code{\link[=scan_prompt]{scan_prompt()}}.}

\item{output_report}{A \code{scan_report} from \code{\link[=scan_output]{scan_output()}}.}

\item{final_action}{Character. The action that was taken.}

\item{redactions}{A list of redaction log entries.}

\item{...}{Additional named fields to include in the audit record.}
}
\value{
A \code{shield_audit} S3 object.
}
\description{
Constructs a \code{shield_audit} S3 object that captures the full lifecycle
record of one shielded LLM interaction. Used internally by
\code{\link[=secure_chat]{secure_chat()}} and can also be constructed manually for custom
integrations.
}
\examples{
# Create a manual audit record
input_report <- scan_prompt("Safe SDTM question.")
output_report <- scan_output("The AE domain stores adverse events.")

audit <- shield_audit(
  policy       = "pharma_gxp",
  model        = "gemma3:4b",
  provider     = "ollama",
  input_report = input_report,
  output_report = output_report,
  final_action = "allow",
  redactions   = list()
)
audit

}
\seealso{
\code{\link[=write_audit_log]{write_audit_log()}}, \code{\link[=secure_chat]{secure_chat()}}
}
